@model IEnumerable<DBTuntiLeimaus.ViewModels.LeimausViewModel>

@{
    ViewBag.Title = "TuntiRaporttiOppilas";
}

<h2>TuntiRaporttiOppilas</h2>
<link href="~/Content/Leimaus.css" rel="stylesheet" />
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>

        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sisaan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ulos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nimi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td class="small">
            @Html.ActionLink(" ", "Edit", null, new { id = item.ID }, new { @class = "fas fa-pencil-alt" })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sisaan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ulos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nimi)
        </td>
        <td>
            @Html.ActionLink(" ", null, new { id = item.ID }, new { @class = "fas fa-trash-alt" })
        </td>
    </tr>
}

</table>
